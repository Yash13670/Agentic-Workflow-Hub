kind = "web"
previewPath = "/"
title = "Autonomous Enterprise Workflow Engine"
version = "1.0.0"
id = "artifacts/workflow-engine"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21348

[services.development]
run = "pnpm --filter @workspace/workflow-engine run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/workflow-engine", "run", "build" ]
publicDir = "artifacts/workflow-engine/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21348"
BASE_PATH = "/"
